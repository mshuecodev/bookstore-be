const path = require("path")

module.exports = {
	config: path.resolve("src/config", "config.js"), // Path to your database config file
	modelsPath: path.resolve("src/models"), // Path to your models directory
	migrationsPath: path.resolve("src/migrations"), // Path to your migrations directory
	seedersPath: path.resolve("src/seeders") // Path to your seeders directory
}
